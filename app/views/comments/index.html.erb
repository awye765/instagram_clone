<%= render @comments, post: @post %>

<!-- This renders each of our comments via the _comment.html.erb partial that
lives in the views/comments folder.  The @post instance variable is passed in
from the index action for the sake of the attached delete feature -->
