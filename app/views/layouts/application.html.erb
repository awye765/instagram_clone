<!DOCTYPE html>
<html>
  <head>
    <title>InstagramClone</title>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>

  <body>

    <nav class="navbar navbar-default">

      <div class="navbar-container">

        <div class="navbar-header">
          <button class="navbar-toggle collapsed" data-target="#bs-navbar-collapse-1" data-toggle="collapse" type="button">
            <span class="sr-only">Toggle Navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <div class="navbar-brand">
            <%= link_to "Photogram", root_path %>
          </div>
        </div>

        <div class="collapse navbar-collapse" id="bs-navbar-collapse-1">
          <ul class="nav navbar-nav navbar-right">
            <li>
              <%= link_to "New Post", new_post_path %>
            </li>
            <li>
              <%= link_to "Login", '#' %>
            </li>
            <li>
              <%= link_to "Register", new_user_registration_path %>
            </li>
          </ul>
        </div>

      </div>

    </nav>
    <div class="container">
      <% flash.each do | name, msg | %>
      <!-- The "name" param is the type of flash message, e.g. :success, :alert,
      etc -->
      <%= content_tag :div, msg, class: [:alert, alert_for(name)] %>
      <!-- "content_tag" is a RAILs method that returns an HTML block.  The type
      of HTML block is specified as <div> in the above and will contain the
      message content of the flash, e.g. "Your post has been created". -->
      <!-- The <div> class is specified as (1) "alert" and (2) by the custom
      helper method, "alert_for(name)", which takes the type of flash message,
      e.g. :success, :alert, etc, as the "name" param and assigns a BOOTSTRAP
      class which will turn that flash a particular colour, e.g. green for
      success. -->

      <% end %>
      <%= yield %>
    </div>

  </body>

</html>
