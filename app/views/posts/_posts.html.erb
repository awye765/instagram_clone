<% @posts.each do | post | %>
  <%= render 'post', post: post %>
  <!-- Interates through each post in the @posts collection, rendering the
  _post.html.erb partial each time.  This is necessary for use in conjunction
  with AJAZ and jQuery to make the button load new pages of posts. -->
<% end %>
